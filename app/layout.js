export const metadata = {
  title: 'Cricket Scorer - Ball by Ball Tracking',
  description: 'Professional cricket scoring app with ball-by-ball tracking',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
